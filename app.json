{
  "name": "diabetes-monitor",
  "description": "Simple app for logging glucose and insulin levels.",
  "keywords": [],
  "website": "",
  "repository": "https://github.com/louis-vs/diabetes-monitor",
  "scripts": [],
  "env": {
    "RACK_ENV": {
      "description": "Rack environment",
      "required": true
    },
    "RAILS_ENV": {
      "description": "Rails environment",
      "required": true
    },
    "RAILS_LOG_TO_STDOUT": {
      "description": "Whether Rails logs to standard output",
      "required": true
    },
    "RAILS_MASTER_KEY": {
      "description": "Key used to decrypt YAML with credentials",
      "required": true
    },
    "RAILS_MAX_THREADS" : {
      "description": "Maximum number of Puma threads.",
      "required": true
    },
    "RAILS_SERVE_STATIC_FILES": {
      "description": "Whether the app should serve static files.",
      "required": true
    },
    "WEB_CONCURRENCY": {
      "description": "The number of processes to run.",
      "required": true
    }
  },
  "addons": [
    "heroku-postgresql:hobby-dev",
    "papertrail:choklad"
  ]
}
